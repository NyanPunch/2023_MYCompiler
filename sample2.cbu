시작

a:=2;
b:=2;
만약에 a 는 b 보다 같으면 a:=5;이다

가:=3;
나:=3;
만일 나 는 가 보다 같다면
가:=6;
이다

다:=9;
라:=9;
만일 다 는 라 보다 작다면
다:=10;
이다

만약에 다 는 라 보다 작거나같으면
다:= 15;
이다

출력 다;

A:=10;
B:=3;
C:=10;
만일 A 는 B 보다 다르면
	만일 A 는 C 보다 다르면
		A:=15;
		출력 A;
	이다
	출력 A;
이다

만약에 A 더하기 B 는 C 보다 크다면 C:=A 더하기 B; 이다

끝